//: Playground - noun: a place where people can play

import UIKit


let _set = Set<String>(["Edit", "Done"])

_set.makeIterator().next()

/*
for (index, item) in _set.makeIterator(). {
    print(index)
    pring(item)
}
 */